# Copy this file to .env and fill in your Supabase project's values
# (Project Settings -> API in the Supabase dashboard). .env is gitignored —
# never commit real keys. Without these, the Shared journal feature is
# simply unavailable; the rest of the app works exactly as before.
SUPABASE_URL=
SUPABASE_ANON_KEY=
