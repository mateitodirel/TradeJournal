// Stub — replaced by Stream 1 with the layered canvas (aurora blobs + starfield
// + pulsing ring). Kept as a mounted no-op so App.tsx wiring lands first.
export function AmbientBackground() {
  return null
}
