/**
 * Central re-export of the lucide-react icons used across the app.
 *
 * Import icons from HERE, never from `lucide-react` directly and never with a
 * namespace import — this keeps the icon set small and tree-shakeable and gives
 * one place to see (and cap) what ships. Default usage:
 *   <Home size={18} strokeWidth={1.75} absoluteStrokeWidth />
 */
export {
  // nav / shell
  CandlestickChart,
  House,
  Search,
  Plus,
  User,
  Bell,
  Settings,
  MoreHorizontal,
  // sections
  LayoutGrid,
  BookOpen,
  NotebookPen,
  Table2,
  Crosshair,
  Sparkles,
  // widgets / actions
  TrendingUp,
  TrendingDown,
  CalendarDays,
  Target,
  Flame,
  ArrowRight,
  Download,
  Upload,
  X,
  Check,
  Pencil,
  Pencil as Edit,
  ChevronRight,
} from 'lucide-react'
